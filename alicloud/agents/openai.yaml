interface:
  display_name: "Alibaba Cloud"
  short_description: "Operate every Alibaba OpenAPI through guarded MCP calls"
  default_prompt: "Use $alicloud to inspect or operate Alibaba Cloud resources safely."
dependencies:
  tools:
    - type: "mcp"
      value: "cloud-skills-mcp"
      description: "Local universal six-cloud MCP server"
policy:
  allow_implicit_invocation: true

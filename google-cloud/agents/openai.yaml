interface:
  display_name: "Google Cloud"
  short_description: "Operate Google Cloud APIs through guarded REST, gRPC, WSS, or SSE"
  default_prompt: "Use $google-cloud to inspect or operate GCP resources safely."
dependencies:
  tools:
    - type: "mcp"
      value: "cloud-skills-mcp"
      description: "Local universal six-cloud MCP server"
policy:
  allow_implicit_invocation: true

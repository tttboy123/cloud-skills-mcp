interface:
  display_name: "Tencent Cloud"
  short_description: "Operate every Tencent API 3.0 action through MCP"
  default_prompt: "Use $tencent-cloud to inspect or operate Tencent Cloud resources safely."
dependencies:
  tools:
    - type: "mcp"
      value: "cloud-skills-mcp"
      description: "Local universal six-cloud MCP server"
policy:
  allow_implicit_invocation: true

interface:
  display_name: "Azure Cloud"
  short_description: "Operate Azure REST APIs through guarded Azure Identity"
  default_prompt: "Use $azure-cloud to inspect or operate Azure resources safely."
dependencies:
  tools:
    - type: "mcp"
      value: "cloud-skills-mcp"
      description: "Local universal six-cloud MCP server"
policy:
  allow_implicit_invocation: true

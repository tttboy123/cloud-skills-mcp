interface:
  display_name: "Baidu AI Cloud"
  short_description: "Operate BCE HTTPS and guarded RTC AI Agent WSS"
  default_prompt: "Use $baidu-cloud to inspect or operate BCE resources safely."
dependencies:
  tools:
    - type: "mcp"
      value: "cloud-skills-mcp"
      description: "Local universal six-cloud MCP server"
policy:
  allow_implicit_invocation: true
